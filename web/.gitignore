node_modules/
.svelte-kit/
build/
.env
.env.*
!.env.example
.DS_Store
cypress/videos/
cypress/screenshots/
cypress/downloads/

# Word add-in build output (generated by `cd word-addin && npm run build`;
# see word-addin/README.md for the build flow). Not tracked in git — the
# contributor's local build or the image-build stage populates this.
static/word-addin/
