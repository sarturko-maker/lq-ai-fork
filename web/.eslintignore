node_modules/
.svelte-kit/
build/
static/
package-lock.json

# F1-S2: vendored shadcn-svelte component source (upstream-formatted;
# provenance in NOTICES.md) — exempt from house lint/format.
src/lib/components/ui/
src/lib/utils.ts

# AE0 (ADR-F011): vendored AI Elements component source. eslint-exempt for the
# same reason as ui/* — the shadcn `...rest` + `$props()` pattern trips
# `svelte/valid-compile`'s custom_element_props_identifier rule, a false
# positive for our SPA (we never compile custom elements). Kept prettier-
# formatted (owned source; provenance in NOTICES.md + ai-elements/README.md).
src/lib/lq-ai/components/ai-elements/
