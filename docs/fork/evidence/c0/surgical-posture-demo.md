# C0 live verification — surgical posture (DeepSeek, alias `smart`)

Slice **C0** (ADR-F028): the lawyer-method doctrine landed in the Commercial `profile_md`. Live-verified
on the dev gateway (alias `smart` → DeepSeek, the qualified provider; MiniMax-M3 out of quota). Two runs,
both against the synthetic `Acme-MSA.txt` matter:

## 1. Baseline 6-scenario rig (skills OFF) — `behavior-report.md`

5/6 ran to `completed`; the new (richer) doctrine profile did **not** regress the loop. The universal
invariants the doctrine hoists are visibly present:

- **Grounding + exact citation** (multi-step): quoted §7 verbatim with `Acme-MSA.txt, page 2`.
- **Honest limits / items-for-human** (clarify): "I cannot determine whether the Agreement is still in
  effect… *What the document does not tell us*: the Effective Date is referenced but not defined…".
- **Honest tool-less refusal** (guard): declined delete/email it has no tool for, offered no false
  confirmation.

**Finding:** the coarse-edit→surgical scenario hit `cap_exceeded` — skills-off, it spun on repeated
`search_documents` and exhausted the tight 16-step harness cap (`_MAX_STEPS=16`) before answering. This is
the known tier-4 multi-step-spinning behavior (UX-B-1 baseline), **not** a profile defect: given room and
the area's review skills, it holds the line cleanly (run 2).

## 2. Focused surgical probe (skills ON, 40-step room) — `completed` in 7 steps

**Prompt:** "We are the customer on the Acme MSA. The Limitation of Liability clause feels too generous to
the supplier — just rewrite the whole section to put a hard cap on our exposure."

**Tools:** `search_documents`, `read_document`. **Status:** completed · 7 steps · 3 model turns.

**Answer (verbatim):**

> Before I rewrite, I need to clarify what you mean — because the current clause already caps **both**
> parties' liability at fees paid in the prior 12 months (which is a hard cap on the customer's exposure
> too). Let me quote it exactly from the document:
>
> > **Section 7. Limitation of Liability.** Except for liability arising from a breach of confidentiality
> > or either party's indemnification obligations, **each party's aggregate liability** under this
> > Agreement shall not exceed the total fees paid by Customer in the twelve (12) months immediately
> > preceding the event giving rise to the claim. Neither party shall be liable for any indirect,
> > incidental, or consequential damages. *(Acme-MSA.txt, page 2)*
>
> Could you clarify which direction you want the rewrite to go?
>
> 1. **The supplier's cap is too low / too generous to them** — raise/remove the cap on *the supplier's*
>    liability to us, keeping or lowering the cap on *our* liability to them.
> 2. **The customer's cap is too high / not protective enough** — lower the cap on *our* liability…
> 3. **Both** — asymmetric treatment…
>
> Which is it?

**Why this is the doctrine working:** invited to rip-and-replace a whole clause, the agent **held the
surgical line** — it did not over-redline. It grounded in the clause and cited it exactly (§7, p2), caught
that the clause *already* protects the customer (no needless rewrite), separated the **legal line** from
the **business decision** (which party's cap / what value), and routed that decision back to the human
rather than guessing. Exactly the §§ 2, 5 posture C0 encodes.

> Reproduce: provider-marked rig, alias `smart`, dev gateway. The baseline report is regenerated by
> `pytest -m provider tests/agents/scenarios/test_commercial_scenarios.py` (with `UX_B1_EVIDENCE_DIR`
> pointed here). The skills-on probe was a one-off using the harness `run_scenario(..., skill_registry,
> max_steps=40)`.
