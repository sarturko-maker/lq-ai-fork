# C5a — provable negotiation round (live evidence)

ADR-F032. The commercial agent reads the **counterparty's marked-up `.docx`** (their tracked changes
+ comments) and responds to **every** change/comment under the code-enforced no-silent-action gate
(`extract_counterparty_position` → `respond_to_counterparty`). Generated by
`tests/agents/scenarios/test_commercial_negotiation_scenario.py` (provider-marked) on the live dev
stack (DeepSeek, `deepseek` = deepseek-v4-flash), 2026-06-24.

## Result (`negotiation-report.json`)

- `status: completed`, `step_count: 7`, `model_turns: 3`, `latency_s: 34.6`.
- `tools_called: [extract_counterparty_position, respond_to_counterparty]` — the negotiation loop ran.
- 4 counterparty changes + 1 comment; **full coverage held in one pass** (the response could not have
  been produced otherwise — the coverage gate rejects any unaddressed/duplicate/unknown ref).

## What the agent decided (`response-reconstruction.txt`)

| Ref | Counterparty change | Decision |
|-----|---------------------|----------|
| C1 | "designated as confidential" → "…in writing" | **accept** (benign clarification) |
| C2 | "Each party shall protect the other party's…" → "The Recipient shall protect the Discloser's…" | **reject** — reverted to MUTUAL |
| C3 | add "…except to its affiliates and advisers" | **accept** (standard, mutual benefit) |
| C4 | "three (3) years…" → "perpetuity" | **escalate** — left as a visible tracked change, **not silently conceded** (below our 3–5yr floor) |
| Com:1 | "this should stay mutual" | **reply** |

The escalate is the load-bearing proof: a below-floor demand was **not** accepted and **not** silently
dropped — it remains a visible tracked change for the supervisor, and the decision is recorded (audit +
matter-memory receipt). Silent-accept is impossible (coverage gate); silent-reject is impossible (a
reject is a recorded `RejectChange`, here reverting C2 to mutual).

## Files

- `Mutual NDA (counterparty markup).docx` — the seeded counterparty markup (input).
- `counterparty-state-of-play.txt` — the markup as the agent reads it (the coverage checklist).
- `Mutual NDA (counterparty markup) (response).docx` — the agent's response (our tracked changes +
  threaded comment reply), matter-scoped, downloadable via C7a.
- `response-reconstruction.txt` — readable `[-del-][+ins+]` view of the response.
- `negotiation-report.json` — the run receipt.
