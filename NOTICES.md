# NOTICES — Upstream Skill Provenance

> **Purpose:** Track the upstream provenance of every skill ported into
> this repository from an external source. Skills authored originally by
> LQ.AI contributors do not appear here. Skills ported from third-party
> corpora (law firm knowledge bases, open-source legal-AI repositories,
> academic datasets, etc.) must have a row in this table.
>
> **Governed by:** [CONTRIBUTING.md — Porting skills from external sources](CONTRIBUTING.md#porting-skills-from-external-sources)
> and [skills/CONTRIBUTING.md](skills/CONTRIBUTING.md).
>
> **How to add a row:** When porting a skill, add a row here in the same
> PR that adds the skill. All five columns are required. "None" is a valid
> value for Attribution requirement when the licence does not impose one
> (e.g., Apache-2.0, MIT). Leave Notes blank when there is nothing material
> to record.

---

## Ported Skills

| Skill scope | Upstream source | License | Attribution requirement | Notes |
|---|---|---|---|---|
| `skills/community/**` | https://github.com/LegalQuants/lq-skills | Per-skill LICENSE in each skill folder | Authors named in SKILL.md frontmatter `author` field | Git submodule pinned to a specific commit; bump via `git submodule update --remote skills/community` |

---

## Format reference

| Column | What to put here |
|---|---|
| **Skill slug** | The slug as it appears in `skills/<slug>/` and the `slug:` frontmatter field. |
| **Upstream source** | Full URI of the upstream repository, document, or dataset. If the source has no stable URI, use a human-readable description (e.g., "Smith & Jones LLP internal knowledge base, licensed for open-source use 2026-04-01"). |
| **License** | [SPDX identifier](https://spdx.org/licenses/) (e.g., `Apache-2.0`, `CC-BY-4.0`). If the licence is not SPDX-enumerated, describe it briefly (e.g., "Custom permissive; see upstream README"). |
| **Attribution requirement** | The exact attribution text required by the licence, or "None". For CC licences, copy the required credit line here. |
| **Notes** | Any material caveats: jurisdiction limitations not captured in frontmatter, version-specific notes, contact for licence questions, etc. |

---

## Amendment procedure

To update a row (e.g., if a upstream source moves or a licence changes):
1. Open a PR with the updated NOTICES.md row.
2. Include the reason for the change in the PR description.
3. If the licence change is restrictive (e.g., the upstream relicensed from permissive to copyleft), flag the maintainer team for review before merging.

---

## Web client provenance

> Added in F0-S6 (ADR-F006). The sections above are skill provenance and are
> governed by their own procedure; this section records the web client's lineage.

| Component | Upstream source | License | Status / Notes |
|---|---|---|---|
| `web/` OpenWebUI husk (fork of open-webui v0.9.2 per ADR-0001) | https://github.com/open-webui/open-webui @ v0.9.2 | Open WebUI License (BSD-3-Clause + §4 branding clause); earlier ranges MIT / BSD-3-Clause per the removed `web/LICENSE_HISTORY` | **REMOVED in F0-S6** (commit `8ec1fca`). Full license texts preserved in git history at `8ec1fca^`. The §4 branding obligation applies to any build from pre-removal commits or tags; it does not apply to the standalone app. |
| `web/` standalone SvelteKit app (post-F0-S6) | Original LQ.AI work; per-file provenance pass over all 123 surviving `src/{lib,routes}/lq-ai` components completed 2026-06-11 (`docs/fork/evidence/f0-s6/provenance.md`) — none OpenWebUI-derived; `app.html` theme script rewritten, not copied | Apache-2.0 (repo-root `LICENSE`) | Bundled third-party assets: Inter Variable font via `@fontsource-variable/inter` (**OFL-1.1** — the published npm package self-declares OFL-1.1 and ships the license text alongside the font files); remaining runtime deps (marked MIT, dompurify Apache-2.0/MPL-2.0 — Apache-2.0 terms elected, eventsource-parser MIT) ship per their permissive licenses, recorded in `web/package.json` + lockfile (the runtime image carries only the built bundle — generate source-level SBOMs from the repo, e.g. `make sbom`, not from the image). |
| `web/src/lib/components/ui/**` + `web/src/lib/utils.ts` vendored shadcn-svelte component source (F1-S2, ADR-F006) | https://github.com/huntabyte/shadcn-svelte — generated by `shadcn-svelte@1.3.0` CLI (`init` preset `b0` nova/neutral + `add` of 11 components), committed verbatim; the CLI is NEVER run in CI — re-running `update`/`add` is an explicit, reviewed change | MIT (upstream `LICENSE.md`) | Vendored source is exempt from house lint/format (`.eslintignore`/`.prettierignore`) to stay diffable against upstream. New devDependencies introduced (build-time; the runtime image carries only the built bundle): `shadcn-svelte` MIT (its `dist/tailwind.css` compiles into the bundle), `bits-ui` MIT, `paneforge` MIT, `@lucide/svelte` ISC (^1.17.0, lockfile-resolved at 1.17.0 — registry items pin an older major; do not let `add` runs rewrite the range downward), `tailwind-variants` MIT, `tailwind-merge` MIT, `tw-animate-css` MIT, `clsx` MIT, `@internationalized/date` Apache-2.0. Transitives: `@floating-ui/{core,dom,utils}` MIT, `tabbable` MIT, `dequal` MIT, `lz-string` MIT, `style-to-object` + `inline-style-parser` MIT, `@swc/helpers` Apache-2.0, `node-fetch-native` MIT (CLI-only), `runed` MIT, `svelte-toolbelt` MIT (svelte-toolbelt's published package.json lacks a `license` field — MIT per the LICENSE file shipped in the tarball; both © Hunter Johnston & Thomas G. Lopes). |
| `web/src/lib/lq-ai/components/ai-elements/**` vendored Svelte AI Elements component source (AE0–AE7, ADR-F011) | https://github.com/SikandarJODD/ai-elements ("Svelte AI Elements") — a shadcn-svelte registry served at `https://svelte-ai-elements.vercel.app/r/<component>.json`; each item's JSON was inspected (deps / registryDependencies / source) then committed with the CLI alias placeholders substituted (`$COMPONENTS$`→`$lib/components`, `$UTILS$`→`$lib/utils`) and house-formatted. AE0 vendored `loader` + `suggestion`; AE1 vendored `conversation` (full block) + the `message` **core layout primitives only** (`Message` + `MessageContent`; the upstream block's branching/attachments/actions/Streamdown-`response` were NOT taken); AE2 vendored the `message` **`actions/` subtree** (`message-action`/`message-actions`/`message-toolbar`) and, per **ADR-F011 option-2**, hand-built the AE **Reasoning** *identity* onto our existing accessible `<details>` `primitives/ReasoningRibbon.svelte` rather than vendoring the AE `reasoning` block (whose registry item pulls `streamdown-svelte` + `@shikijs/themes` + `mode-watcher` + a `collapsible` we don't ship + the Streamdown `Response` sink — all avoided). AE3 vendored `sources/source.svelte` faithfully (book-icon entry, `href` made optional for our internal docs) and the two **dependency-free** `inline-citation/` primitives this slice uses (`-source`/`-quote`; the `-text`/wrapper + the hover-card/carousel pieces were skipped — "take only what you need"), and per **ADR-F011 option-2** hand-built the AE **Sources** collapsible ("Used N sources") onto the native `<details>` (the upstream Sources trio + the inline-citation `Card`/`Carousel` pull `collapsible` + `hover-card` + `carousel` — all avoided). | MIT (upstream `LICENSE`) | A faithful Svelte port of Vercel's React AI Elements; we adopt only its presentation (ADR-F011) — NOT `@ai-sdk/svelte`'s `Chat` transport, and NOT its markdown sink (we keep `marked`+`DOMPurify` via `renderModelMarkdown`). **No new runtime dependencies** through AE3: `loader` pulls nothing; `suggestion`/`conversation` reuse already-vendored `ui/button` + `ui/scroll-area` + `cn` + `@lucide/svelte` (ISC); the AE2 `actions/` reuse already-vendored `ui/button` + `ui/tooltip` + `cn` + `@lucide/svelte`; the AE3 `sources/` + `inline-citation/` reuse only `cn` + `@lucide/svelte`. `runed` (MIT) was previously a shadcn transitive and is now a declared `devDependency` (`^0.35.1`) since `conversation/` imports it first-party (build-time; the runtime image carries only the built bundle). Components are born `0 var(--lq-)` (no R-LAST regression). eslint-exempt (custom-element false positive, same as `ui/*`); kept prettier-formatted (owned source — re-tokened/re-wired as the AE series lands; see `web/src/lib/lq-ai/components/ai-elements/README.md`). AE4 added `shiki` (code highlighting) — recorded in the row below. AE5 (Prompt Input) vendored NO new AE source and added NO new dep: the `prompt-input` registry item pulls `ai@^6` (the AI SDK transport we reject) + `runed` + 6 registry deps + 23 SDK-bound context files, so per **ADR-F011 option-2** the AE Prompt Input identity was hand-built directly on the existing composer in `ChatPanel.svelte` (lucide toolbar icons), and ChatPanel's header + composer were migrated off `--lq-*` to semantic tokens (the dark-mode column-gap fix). AE6 (Tool + Task) likewise vendored NO new AE source and added NO new dep: the `tool` registry item pulls `collapsible` + `badge` + `runed` + `./code.json` (the code block we hand-built in AE4) and `task` pulls `collapsible` + `bits-ui`, so per **ADR-F011 option-2** the AE **Tool** card (wrench header + status badge + Parameters/Result) and **Task** step-list identity were hand-built on native `<details>` directly in `ConversationPanel.svelte` (lucide `wrench`/`search`/`circle-check`/`loader-circle`/`chevron-down`); the same slice converged `ConversationPanel` + `SkillSourceView` off their local `marked`+`DOMPurify` copies onto the shared `renderModelMarkdown` sink. AE7 (Suggestions, the AE-series closer) reused the AE0-vendored `suggestion/` (`Suggestion`/`Suggestions`) as-is and added NO new dep: the chips are rendered in `ChatPanel.svelte` as empty-conversation STARTERS backed by the caller's own `SavedPrompts` (an honest, user-owned data source surfaced from `SavedPromptsPanel`'s single existing fetch via a new `onPromptsLoaded` callback — no duplicate request) — NOT model-invented follow-ups (no honest source for those exists, so none are shown; an empty saved-prompts list renders no chips). |
| `web/src/lib/lq-ai/code/**` AE4 code-block highlighting (ADR-F011, option-2 hand-build — NOT vendored AE source) | Original LQ.AI work. The AE `code` registry item was inspected (`https://svelte-ai-elements.vercel.app/r/code.json`) and **not vendored**: it pulls `svelte-toolbelt` + a separate `copy-button` registry item + line-number/overflow machinery, and its controlled-`code`-prop API does not fit our `renderModelMarkdown` → single `{@html}` sink. Per **ADR-F011 option-2** the AE code-block *identity* (bordered card, language header, copy button, Shiki GitHub light/dark dual-theme) is hand-built as a Svelte action that post-processes the already-sanitized `<pre><code>` output (mirrors `citations/decorate-inline.ts`). | Apache-2.0 (repo-root `LICENSE`) | **New runtime dependency (ADR-F011 §"New dependencies, justified" pre-approved it): `shiki` MIT (`^4.2.0`)** — a syntax tokenizer (no `eval`, no network; turns a source string into styled `<span>`s). Used in the *fine-grained* form (bundled `createHighlighter` + the pure-JS regex engine `shiki/engine/javascript`, no WASM, an explicit short language list). Highlight runs client-side on already-sanitized text and Shiki's output is **re-sanitized with DOMPurify** before it re-enters the DOM — no new injection sink. Transitives are the shiki monorepo (`@shikijs/{core,engine-javascript,engine-oniguruma,langs,themes,types,vscode-textmate,primitive}`) + its rehype/oniguruma syntax-tree stack (`hast-util-to-html`, `mdast-util-to-hast`, `micromark-util-*`, `unist-util-*`, `oniguruma-to-es`/`regex*`, `property-information`, `vfile*`, etc.), all MIT, recorded in `web/package.json` + lockfile (the runtime image carries only the built bundle). |
| `web/src/lib/lq-ai/components/ropa/DataFlowView.svelte` + `DataFlowNodeCard.svelte` data-flow / lineage graph (PRIV-6c, ADR-F022) | Original LQ.AI work; renders the backend's lib-agnostic `DataFlowGraph` projection (`/ropa/data-flow`) through our own F013 node card. | Apache-2.0 (repo-root `LICENSE`) | **New runtime dependency (ADR-F022 — the fork's first deliberate exception to the no-new-dep default; maintainer-authorised): `@xyflow/svelte` MIT (`^1.6.0`, lockfile-resolved 1.6.0)** — a Svelte-5-native node-link graph renderer (Svelte Flow, by the xyflow team / makers of React Flow). We supply our own custom node component so the look stays F013 — only the canvas/transform/edges are borrowed; node positions are computed in our own pure `dataFlow.ts` (no dagre/elk layout dep). Direct transitives: `@xyflow/system` MIT (which pulls `d3-drag`/`d3-zoom`/`d3-selection`/`d3-interpolate`, all ISC, + their `@types/*`) and `@svelte-put/shortcut` MIT — all permissive (MIT/ISC/BSD), recorded in `web/package.json` + lockfile (the runtime image carries only the built bundle). Renderer is swappable: the projection DTO + endpoint are library-agnostic. |

---

## Python runtime dependencies (license posture)

> The authoritative Python SBOM is `api/pyproject.toml`, where every runtime
> dependency carries an inline rationale + license comment. This table records
> the *license posture* for dependencies where it is load-bearing: the single
> copyleft dependency and its server-side boundary, and the permissive
> document-reader libraries added by the COMM milestone (CLAUDE.md rule B —
> **no copyleft in new deps**).

| Dependency | Used by | License | Posture |
|---|---|---|---|
| `pymupdf` (PyMuPDF / `fitz`) | `api/app/pipeline/parsers.py` — PDF only | **AGPL-3.0** | Server-side-only boundary (ADR-0006 § License posture; PRD §7.2 / §1519). The **only** copyleft dependency; pre-dates rule B (grandfathered). C1 (ADR-F029) contains it behind `PdfReader` and adds a **CI import-guard test** asserting no other reader imports `fitz`, so the AGPL surface does not widen. Replacement with a permissive PDF parser is COMM open question #1 (maintainer's call). |
| `python-docx` | `api/app/pipeline/readers/docx.py` — DOCX (C1, ADR-F029) | MIT | Permissive. Transitive `lxml` BSD-3 (already in-tree). |
| `python-pptx` | `api/app/pipeline/readers/pptx.py` — PPTX (C1, ADR-F029) | MIT | Permissive. Transitives `lxml` BSD-3, `Pillow` HPND, `XlsxWriter` BSD-2. |
| `openpyxl` | `api/app/pipeline/readers/xlsx.py` — XLSX (C1) + Tabular export | MIT | Permissive (already in-tree; no new dep for C1). |
| `python-oxmsg` | `api/app/pipeline/readers/msg.py` — Outlook `.msg` (C2, ADR-F029) | MIT | Permissive. Read-only OLE parsing (no network/eval; egress proven offline). Transitives `olefile` BSD-2, `click` BSD-3. Pinned exact (`==0.0.2`, young package). NOT GPLv3 `extract-msg`. |
| `adeu` | `api/app/agents/redline_service.py` — native tracked-changes `.docx` redline (C4, ADR-F031) | MIT | Permissive. Integrated via its Python **SDK in-process** (`RedlineEngine` / `ModifyText` / `process_batch`) — **never** its bundled FastMCP server (`adeu.server` / `adeu.mcp_components`), a second egress. Installed **`--no-deps`** (api/Dockerfile, api/Dockerfile.dev, ci.yml): its hard `fastmcp[apps]` dep would pull starlette 0.48 / mcp / pydantic 2.13 and break our pinned FastAPI stack, and we don't use the server — so `fastmcp` is **not installed** and `adeu.server` is a hard `ModuleNotFoundError` (the second egress is structurally absent, not just guarded by the import test). Its real SDK deps are declared in `pyproject.toml`: `diff-match-patch` **Apache-2.0** (word-diff engine), `structlog` MIT/Apache, plus `lxml` BSD-3 / `python-docx` MIT / `rapidfuzz` MIT / `pydantic` MIT (already in-tree). Makes **zero** provider/network calls (verified offline at C-R0). Pinned exact (`==1.12.1`). NO new copyleft (rule B). |

---

## Runtime service images (license posture)

> Containerized runtime services pulled as images (not Python/web source deps).
> Recorded where the license posture is load-bearing.

| Image | Used by | License | Posture |
|---|---|---|---|
| `collabora/code` (Collabora Online Development Edition; **LibreOffice** core on LibreOfficeKit) | The `collabora` compose service — the in-app Word editor engine (ADR-F047; libreoffice-editor Slice 1) | **MPL-2.0** (Collabora Online / COOL source); LibreOffice core **dual MPL-2.0 / LGPLv3+**; COOL `browser/` **BSD**; bundled fonts **OFL-1.1**; dictionaries **MPL-1.1** | **Weak / file-level copyleft only — NO AGPL, no network copyleft** (strictly lighter than the grandfathered PyMuPDF AGPL-3.0 row above). Run as a **separate, unmodified, server-side-only container** (WOPI host will live in `api`, iframe in `web`; the editor data plane makes **no** LLM/gateway call — the ADR-F010 egress invariant is untouched), pinned **by digest**. The MPL-2.0 *source* carries **no** production restriction. The dev/integration stack runs the **prebuilt official CODE binary**, whose *executable* form additionally carries a proprietary Collabora EULA + Collabora trademark/CSS and a "not recommended for production" framing (a support/warranty stance, **not** a license prohibition; the historical 10-document / 20-connection cap is **removed** in current CODE). We hide Collabora chrome via our own UI + CSS config (officially permitted for self-hosted installs) and do **not** redistribute the binary. The clean unbranded / supported **production** posture is a **deferred decision** — self-build from MPL-2.0 source **or** a Collabora subscription — to be made at productionisation (`docs/fork/MILESTONES.md`). **NOT AGPL.** |
